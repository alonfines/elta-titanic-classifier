# Lower bounds only, not a lockfile — tested against the versions in parentheses (2026-08-17).
# For an exact reproduction of the dev environment, pin to those instead.

# Data fetching
kaggle>=2.2.4

# Data handling (also used by the notebook, train.py, and the Streamlit app)
pandas>=2.2       # tested: 3.0.5
numpy>=1.26        # tested: 2.5.2

# EDA notebook
matplotlib>=3.8
seaborn>=0.13
jupyter>=1.0
ipykernel>=6.29
nbconvert>=7.16

# Preprocessing + model (train.py, and reused by ds_app.py for inference)
scikit-learn>=1.4  # tested: 1.9.0
torch>=2.2           # tested: 2.13.0

# Evaluation + inference UI
streamlit>=1.32    # tested: 1.61.1
